require './test/testutil/requires'

class FunctionallyCodingTests < FreshDatabaseTestCase
  should "blah" do
    # r = Reservation.random(:animal => Animal.random,
    #                        :procedure => Procedure.random)
    
    # functionally = Functionally.new
    # functionally.shift_to_timeslice(r.id, "blah")
  end
end
