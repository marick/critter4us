%div.centered
  Schedule copies of this reservation:
  =partial_call(:long_form_reservation, :reservation => reservation)
  %form#schedule_form{:method => 'POST', :action => Href.schedule_reservations_page(reservation)}
    Repeat every week up through
    %input#weekly_end_date{:type => 'text'}
    %input{:type=>'submit', :value=>"Duplicate"}

:javascript
  jQuery(function() {
    (new window.C4.ReservationSchedulingPage).initialize_jquery()
  });
