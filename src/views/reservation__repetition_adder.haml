%div.centered
  Schedule copies of this reservation:
  =partial_call(:long_form_reservation, :reservation => reservation)
  %div
    Repeat every week up through
    %input#weekly_end_date{:type => 'text'}
    %input#duplicate_by_week{:type=>'submit', :value=>"Duplicate"}
  %div#progress_container

%div#templates
  %div.repetition_progress
    %p.date
    %p.notes
     
:javascript
  jQuery(function() {
    var start = new Date(#{start_date.year}, #{start_date.month-1}, #{start_date.mday});
    (new window.C4.RepetitionAddingPage).initialize_jquery(start);
  });
